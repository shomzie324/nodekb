extends layout

block content
    h1= article.title
    // syntax for text with variables in pug
    h5 Written by #{article.author}
    p= article.body
    hr
    a.btn.btn-default(href='/article/edit/'+article._id) Edit
    a.btn.btn-danger.delete-article(href='#',data-id=article._id) Delete